import { LoggerConfig, LogParams } from "./model";
export declare class Logger {
    private serviceName;
    private logDir;
    private logLevel;
    constructor({ serviceName, logDir, logLevel, }: LoggerConfig);
    log({ type, message, logId, payload, statusCode, sourceURL, targetURL, }: LogParams): void;
}
