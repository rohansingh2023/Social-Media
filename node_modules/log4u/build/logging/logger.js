"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Logger = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const moment_1 = __importDefault(require("moment"));
class Logger {
    constructor({ serviceName, logDir = "logs", logLevel = "INFO", }) {
        this.serviceName = serviceName;
        this.logDir = path_1.default.join(process.cwd(), logDir); // Ensure the path is relative to the project root
        this.logLevel = logLevel;
        // Ensure logs directory exists
        if (!fs_1.default.existsSync(this.logDir)) {
            fs_1.default.mkdirSync(this.logDir);
        }
    }
    log({ type = "INFO", message, logId, payload, statusCode, sourceURL, targetURL, }) {
        if (!message) {
            throw new Error('The "message" parameter is required for logging.');
        }
        // Generate timestamps
        const now = (0, moment_1.default)().format("YYYY-MM-DD HH:mm:ss");
        const date = (0, moment_1.default)().format("YYYY-MM-DD");
        const logFileName = path_1.default.join(this.logDir, `${date}.log`);
        let logMessage = "";
        if (message instanceof Error) {
            logMessage = message.stack || message.message;
        }
        else {
            logMessage = String(message);
        }
        // Build the log entry dynamically (only include values)
        const logValues = [
            type,
            now,
            this.serviceName,
            logMessage,
            logId,
            payload ? JSON.stringify(payload) : undefined,
            statusCode,
            sourceURL,
            targetURL,
        ]
            .filter((value) => value !== undefined) // Exclude undefined values
            .join(" | "); // Join values with a separator
        // Write to log file
        fs_1.default.appendFileSync(logFileName, logValues + "\n");
    }
}
exports.Logger = Logger;
